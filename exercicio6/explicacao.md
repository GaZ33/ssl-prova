# Explicação
O comando `scp aluno.crt usuario@IP_DESTINO:/etc/ssl/certificates/` é utilizado para transferir arquivos entre um computador local e um servidor de forma criptografada, utilizando o protocolo SSH para garantir a segurança da transmissão
